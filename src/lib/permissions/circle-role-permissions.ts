import { CIRCLE_PERMISSIONS, type CirclePermission } from "./circlePermissions"
import type { MemberRole } from "@/generated/prisma"

const P = CIRCLE_PERMISSIONS

const OWNER_PERMISSIONS: CirclePermission[] = Object.values(P)

const ADMIN_PERMISSIONS: CirclePermission[] = [
  P.CIRCLE_VIEW,
  P.CIRCLE_UPDATE,
  P.SETTINGS_MANAGE,
  P.MEMBER_VIEW,
  P.MEMBER_INVITE,
  P.MEMBER_REMOVE,
  P.MEMBER_ROLE_UPDATE,
  P.MEMBER_PERMISSION_MANAGE,
  P.CONTRIBUTION_CREATE,
  P.CONTRIBUTION_SUBMIT_OWN,
  P.CONTRIBUTION_VIEW_OWN,
  P.CONTRIBUTION_VIEW_ALL,
  P.CONTRIBUTION_REVIEW,
  P.CONTRIBUTION_REJECT,
  P.CONTRIBUTION_REFUND,
  P.EXPENSE_CREATE,
  P.EXPENSE_VIEW,
  P.EXPENSE_APPROVE,
  P.EXPENSE_DELETE,
  P.SETTLEMENT_CREATE,
  P.SETTLEMENT_VIEW,
  P.SETTLEMENT_CONFIRM,
  P.GOAL_CREATE,
  P.GOAL_UPDATE,
  P.GOAL_DELETE,
  P.LEDGER_VIEW,
  P.LEDGER_ADJUST,
  P.REPORT_VIEW,
  P.REPORT_EXPORT,
  P.PROJECT_CREATE,
  P.PROJECT_VIEW,
  P.PROJECT_MANAGE,
  P.PROJECT_APPROVE,
  P.WALLET_VIEW,
  P.PAYOUT_REQUEST,
  P.PAYOUT_APPROVE,
  P.FEED_POST,
  P.FEED_DELETE,
  P.FEED_PIN,
  P.EVENT_MANAGE,
  P.POLL_MANAGE,
  P.JOIN_REQUEST_REVIEW,
  P.INVITE_MANAGE,
  P.AUTOMATION_MANAGE,
  P.WORKFLOW_MANAGE,
  P.APPROVAL_WORKFLOW_VIEW,
  P.APPROVAL_WORKFLOW_CREATE,
  P.APPROVAL_WORKFLOW_UPDATE,
  P.APPROVAL_WORKFLOW_ACTIVATE,
  P.APPROVAL_WORKFLOW_ARCHIVE,
  P.APPROVAL_REVIEW_OWN,
  P.APPROVAL_REVIEW_ANY,
  P.APPROVAL_DELEGATE,
  P.APPROVAL_ESCALATE,
  P.APPROVAL_VIEW_ALL,
]

const TREASURER_PERMISSIONS: CirclePermission[] = [
  P.CIRCLE_VIEW,
  P.MEMBER_VIEW,
  P.CONTRIBUTION_SUBMIT_OWN,
  P.CONTRIBUTION_VIEW_OWN,
  P.CONTRIBUTION_VIEW_ALL,
  P.CONTRIBUTION_REVIEW,
  P.CONTRIBUTION_REJECT,
  P.EXPENSE_CREATE,
  P.EXPENSE_VIEW,
  P.EXPENSE_APPROVE,
  P.SETTLEMENT_VIEW,
  P.SETTLEMENT_CONFIRM,
  P.LEDGER_VIEW,
  P.REPORT_VIEW,
  P.REPORT_EXPORT,
  P.PROJECT_VIEW,
  P.WALLET_VIEW,
  P.FEED_POST,
  P.JOIN_REQUEST_REVIEW,
  P.APPROVAL_WORKFLOW_VIEW,
  P.APPROVAL_REVIEW_OWN,
  P.APPROVAL_REVIEW_ANY,
  P.APPROVAL_ESCALATE,
]

const MEMBER_PERMISSIONS: CirclePermission[] = [
  P.CIRCLE_VIEW,
  P.MEMBER_VIEW,
  P.CONTRIBUTION_SUBMIT_OWN,
  P.CONTRIBUTION_VIEW_OWN,
  P.EXPENSE_CREATE,
  P.EXPENSE_VIEW,
  P.SETTLEMENT_CREATE,
  P.SETTLEMENT_VIEW,
  P.GOAL_CREATE,
  P.LEDGER_VIEW,
  P.REPORT_VIEW,
  P.PROJECT_VIEW,
  P.FEED_POST,
  P.EVENT_MANAGE,
  P.POLL_MANAGE,
  P.APPROVAL_REVIEW_OWN,
]

const VIEWER_PERMISSIONS: CirclePermission[] = [
  P.CIRCLE_VIEW,
  P.MEMBER_VIEW,
  P.EXPENSE_VIEW,
  P.SETTLEMENT_VIEW,
  P.LEDGER_VIEW,
  P.REPORT_VIEW,
  P.PROJECT_VIEW,
  P.APPROVAL_WORKFLOW_VIEW,
]

export const CIRCLE_ROLE_PERMISSIONS: Record<MemberRole, CirclePermission[]> = {
  OWNER: OWNER_PERMISSIONS,
  ADMIN: ADMIN_PERMISSIONS,
  TREASURER: TREASURER_PERMISSIONS,
  MEMBER: MEMBER_PERMISSIONS,
  VIEWER: VIEWER_PERMISSIONS,
}

export function getRoleDefaultPermissions(role: MemberRole): CirclePermission[] {
  return CIRCLE_ROLE_PERMISSIONS[role] ?? []
}
